# lib/

## docs-pages.ts

`DOC_PAGES` in `lib/docs-pages.ts` is manually maintained — it must stay in sync with the actual doc routes in `app/docs/`. When adding, renaming, or removing a doc page, update that array and the `PINNED_DOC_URLS` constant in `components/search-command.tsx` if needed.
